node_modules/
coverage/
*.log
.DS_Store
.env

# TypeScript package output is generated locally. The bundled files under
# action/ are intentionally tracked because GitHub does not install dependencies.
dist/
