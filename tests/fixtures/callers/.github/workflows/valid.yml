name: Valid caller

on:
  pull_request:

permissions:
  contents: read

jobs:
  deploy:
    uses: ./.github/workflows/called.yml
    with:
      target: production
      attempts: 2
    secrets:
      token: '${{ secrets.DEPLOY_TOKEN }}'
